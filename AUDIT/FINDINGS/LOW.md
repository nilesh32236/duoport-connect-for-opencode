# LOW — 18 findings
Admin notice imprecision `:32`, error_log without class `:59,66`, get_option per parse `AbstractOpenCodeModelMetadataDirectory:87`, triple-delete `Settings:110-125`, synchronized expiry jitter, init:20 on every request, autoloader file_exists stat, portrait SVG, 16-bit PNG bloat, etc — see AGENTS for full list.
