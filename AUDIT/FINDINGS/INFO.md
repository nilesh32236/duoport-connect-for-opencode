# INFO — 31 observations
PSR-4/camelCase exclusions documented in phpcs.xml, allowlist alive (isAllowed/isFree/displayName), provider pairs 93-97% dup intentional, SDK version gates, WPCS 0 errors with config, no Approvals_Store, connector override pristine restore, no frontend assets — all confirmed pass.
